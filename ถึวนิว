local Config = {Aimbot = true, Smoothness = 1, FOVEnabled = true, FOVRadius = 120, ESPEnabled = true, Speed = 16} local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local Camera = workspace.CurrentCamera local RunService = game:GetService("RunService") local CoreGui = game:GetService("CoreGui") if CoreGui:FindFirstChild("MobileAimbotESP") then CoreGui.MobileAimbotESP:Destroy() end local sg = Instance.new("ScreenGui", CoreGui) sg.Name = "MobileAimbotESP" sg.ResetOnSpawn = false local fovCircle = Instance.new("Frame", sg) fovCircle.BackgroundTransparency = 1 fovCircle.Size = UDim2.fromOffset(Config.FOVRadius * 2, Config.FOVRadius * 2) fovCircle.Position = UDim2.new(0.5, -Config.FOVRadius, 0.5, -Config.FOVRadius) local fovCorner = Instance.new("UICorner", fovCircle) fovCorner.CornerRadius = UDim.new(1, 0) local fovStroke = Instance.new("UIStroke", fovCircle) fovStroke.Thickness = 1.5 fovStroke.Color = Color3.fromRGB(255,255,255) fovStroke.Transparency = 0.3 local LineFolder = Instance.new("Folder", sg) LineFolder.Name = "Snaplines" local camAtt = Instance.new("Attachment", workspace.Terrain) local function GetAnyTarget(model) if not model then return nil end return model:FindFirstChild("Head") or model:FindFirstChild("LowerTorso") or model:FindFirstChild("HumanoidRootPart") end local function GetTarget() local target, maxDist = nil, Config.FOVRadius or math.huge local center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2) local alive = workspace:FindFirstChild("Alive") or workspace:FindFirstChild("Characters") or workspace:FindFirstChild("Arena", true) or workspace if alive then for _, obj in ipairs(alive:GetDescendants()) do if obj:IsA("Model") and obj.Name ~= LocalPlayer.Name then local p = GetAnyTarget(obj) if p then local pos, screen = Camera:WorldToViewportPoint(p.Position) if screen then local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude if dist < maxDist then maxDist = dist target = p end end end end end end return target end local btn = Instance.new("TextButton", sg) btn.Size = UDim2.new(0, 95, 0, 35) btn.Position = UDim2.new(0.05, 0, 0.25, 0) btn.BackgroundColor3 = Color3.fromRGB(235, 94, 40) btn.Text = "SammerHuB" btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.Font = Enum.Font.SourceSansBold btn.TextSize = 14 Instance.new("UICorner", btn) local frame = Instance.new("Frame", sg) frame.Size = UDim2.new(0, 360, 0, 280) frame.Position = UDim2.new(0.3, 0, 0.2, 0) frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25) frame.Visible = true frame.Active = true frame.Draggable = true Instance.new("UICorner", frame) local tl = Instance.new("TextLabel", frame) tl.Size = UDim2.new(1, 0, 0, 35) tl.BackgroundColor3 = Color3.fromRGB(35, 35, 35) tl.Text = "  SAMMERHUB FINAL FIXED V40" tl.TextColor3 = Color3.fromRGB(255, 255, 255) tl.TextXAlignment = Enum.TextXAlignment.Left tl.Font = Enum.Font.SourceSansBold tl.TextSize = 14 Instance.new("UICorner", tl) local function createToggle(txt, pos, default, cb) local b = Instance.new("TextButton", frame) b.Size = UDim2.new(0, 160, 0, 35) b.Position = pos b.BackgroundColor3 = default and Color3.fromRGB(40, 150, 40) or Color3.fromRGB(150, 40, 40) b.TextColor3 = Color3.fromRGB(255, 255, 255) b.Font = Enum.Font.SourceSansBold b.TextSize = 14 b.Text = txt .. (default and ": ON" or ": OFF") local active = default b.MouseButton1Click:Connect(function() active = not active cb(active) b.Text = txt .. (active and ": ON" or ": OFF") b.BackgroundColor3 = active and Color3.fromRGB(40, 150, 40) or Color3.fromRGB(150, 40, 40) end) Instance.new("UICorner", b) end createToggle("Aimbot", UDim2.new(0.05, 0, 0.18, 0), true, function(val) Config.Aimbot = val end) createToggle("Wall ESP", UDim2.new(0.53, 0, 0.18, 0), true, function(val) Config.ESPEnabled = val end) local sBtn = Instance.new("TextButton", frame) sBtn.Size = UDim2.new(0, 160, 0, 35) sBtn.Position = UDim2.new(0.05, 0, 0.38, 0) sBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45) sBtn.TextColor3 = Color3.fromRGB(255, 255, 255) sBtn.Font = Enum.Font.SourceSansBold sBtn.TextSize = 13 sBtn.Text = "Smooth: 1 (Insta)" sBtn.MouseButton1Click:Connect(function() if Config.Smoothness == 1 then Config.Smoothness = 0.05 sBtn.Text = "Smooth: 0.05 (Smooth)" elseif Config.Smoothness == 0.05 then Config.Smoothness = 0.1 sBtn.Text = "Smooth: 0.1 (Medium)" else Config.Smoothness = 1 sBtn.Text = "Smooth: 1 (Insta-Lock)" end end) Instance.new("UICorner", sBtn) local fovBtn = Instance.new("TextButton", frame) fovBtn.Size = UDim2.new(0, 160, 0, 35) fovBtn.Position = UDim2.new(0.53, 0, 0.38, 0) fovBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45) fovBtn.TextColor3 = Color3.fromRGB(255, 255, 255) fovBtn.Font = Enum.Font.SourceSansBold fovBtn.TextSize = 13 fovBtn.Text = "FOV Size: " .. tostring(Config.FOVRadius) fovBtn.MouseButton1Click:Connect(function() if Config.FOVRadius == 120 then Config.FOVRadius = 160 fovBtn.Text = "FOV Size: 160 (Medium)" elseif Config.FOVRadius == 160 then Config.FOVRadius = 220 fovBtn.Text = "FOV Size: 220 (Wide)" else Config.FOVRadius = 120 fovBtn.Text = "FOV Size: 120 (Normal)" end fovCircle.Size = UDim2.fromOffset(Config.FOVRadius * 2, Config.FOVRadius * 2) fovCircle.Position = UDim2.new(0.5, -Config.FOVRadius, 0.5, -Config.FOVRadius) end) Instance.new("UICorner", fovBtn) local speedBtn = Instance.new("TextButton", frame) speedBtn.Size = UDim2.new(0, 320, 0, 35) speedBtn.Position = UDim2.new(0.05, 0, 0.62, 0) speedBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45) speedBtn.TextColor3 = Color3.fromRGB(255, 255, 255) speedBtn.Font = Enum.Font.SourceSansBold speedBtn.TextSize = 13 speedBtn.Text = "Speed Hack: 16 (Normal)" speedBtn.MouseButton1Click:Connect(function() if Config.Speed == 16 then Config.Speed = 50 speedBtn.Text = "Speed Hack: 50 (Fast)" elseif Config.Speed == 50 then Config.Speed = 100 speedBtn.Text = "Speed Hack: 100 (Extreme)" else Config.Speed = 16 speedBtn.Text = "Speed Hack: 16 (Normal)" end end) Instance.new("UICorner", speedBtn) btn.MouseButton1Click:Connect(function() frame.Visible = not frame.Visible end) RunService.RenderStepped:Connect(function() fovCircle.Visible = Config.FOVEnabled local center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2) fovCircle.Position = UDim2.new(0, center.X - Config.FOVRadius, 0, center.Y - Config.FOVRadius) local target = GetTarget() if Config.Aimbot and target then Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Position), Config.Smoothness) end if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid") local root = LocalPlayer.Character.HumanoidRootPart if Config.Speed > 16 and hum.MoveDirection.Magnitude > 0 then root.Velocity = Vector3.new(hum.MoveDirection.X * Config.Speed, root.Velocity.Y, hum.MoveDirection.Z * Config.Speed) end end camAtt.WorldPosition = Camera.CFrame.Position + (Camera.CFrame.LookVector * 2) + (Camera.CFrame.UpVector * 1.5) local alive = workspace:FindFirstChild("Alive") or workspace:FindFirstChild("Characters") or workspace:FindFirstChild("Arena", true) or workspace if alive then for _, obj in ipairs(alive:GetChildren()) do if obj:IsA("Model") and obj.Name ~= LocalPlayer.Name then local root = GetAnyTarget(obj) if root then local bb = obj:FindFirstChildOfClass("BillboardGui") local beam = obj:FindFirstChild("SnapBeam") local tAtt = root:FindFirstChild("TargetAtt") if Config.ESPEnabled then if not bb then bb = Instance.new("BillboardGui", obj) bb.AlwaysOnTop = true bb.Size = UDim2.new(4, 0, 5, 0) bb.Adornee = root local f = Instance.new("Frame", bb) f.Size = UDim2.new(1, 0, 1, 0) f.BackgroundTransparency = 1 local s = Instance.new("UIStroke", f) s.Thickness = 2.5 s.Color = Color3.fromRGB(255, 0, 0) bb.Parent = obj end bb.Enabled = true if not tAtt then tAtt = Instance.new("Attachment", root) tAtt.Name = "TargetAtt" end if not beam then beam = Instance.new("Beam", obj) beam.Name = "SnapBeam" beam.Width0 = 0.06 beam.Width1 = 0.06 beam.Color = ColorSequence.new(Color3.fromRGB(255, 0, 0)) beam.FaceCamera = true beam.Attachment0 = camAtt beam.Attachment1 = tAtt end beam.Enabled = true else if bb then bb.Enabled = false end if beam then beam.Enabled = false end end end end end end end)
