local Config = {Aimbot = false, Smoothness = 0.05, FOVEnabled = true, FOVRadius = 120, ESPEnabled = false} local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local Camera = workspace.CurrentCamera local RunService = game:GetService("RunService") local CoreGui = game:GetService("CoreGui") if CoreGui:FindFirstChild("MobileAimbotESP") then CoreGui.MobileAimbotESP:Destroy() end local sg = Instance.new("ScreenGui", CoreGui) sg.Name = "MobileAimbotESP" sg.ResetOnSpawn = false local fovCircle = Instance.new("Frame", sg) fovCircle.BackgroundTransparency = 1 fovCircle.Size = UDim2.fromOffset(Config.FOVRadius * 2, Config.FOVRadius * 2) fovCircle.Position = UDim2.new(0.5, -Config.FOVRadius, 0.5, -Config.FOVRadius) local fovCorner = Instance.new("UICorner", fovCircle) fovCorner.CornerRadius = UDim.new(1, 0) local fovStroke = Instance.new("UIStroke", fovCircle) fovStroke.Thickness = 1.5 fovStroke.Color = Color3.fromRGB(255,255,255) fovStroke.Transparency = 0.3 local camAtt = Instance.new("Attachment", workspace.Terrain) local function IsVisible(part) if not part then return false end local ray = Ray.new(Camera.CFrame.Position, (part.Position - Camera.CFrame.Position).Unit * 500) local ignore = {LocalPlayer.Character, Camera, workspace.CurrentCamera} local hit = workspace:FindPartOnRayWithIgnoreList(ray, ignore) return hit == nil or hit:IsDescendantOf(part.Parent) end local function GetDeagleTarget(model) if not model then return nil end return model:FindFirstChild("Head") or model:FindFirstChild("LowerTorso") or model:FindFirstChild("HumanoidRootPart") end local function GetTarget() local target, maxDist = nil, Config.FOVRadius or math.huge local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2) local function scan(obj) if obj and obj:IsA("Model") and obj ~= LocalPlayer.Character then local p = GetDeagleTarget(obj) if p and IsVisible(p) then local pos, screen = Camera:WorldToViewportPoint(p.Position) if screen then local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude if dist < maxDist then maxDist = dist target = p end end end end end for _, p in ipairs(Players:GetPlayers()) do scan(p.Character) end for _, obj in ipairs(workspace:GetChildren()) do scan(obj) end local alive = workspace:FindFirstChild("Alive") if alive then for _, obj in ipairs(alive:GetChildren()) do scan(obj) end end return target end local btn = Instance.new("TextButton", sg) btn.Size = UDim2.new(0, 95, 0, 35) btn.Position = UDim2.new(0.05, 0, 0.25, 0) btn.BackgroundColor3 = Color3.fromRGB(235, 94, 40) btn.Text = "SammerHuB" btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.Font = Enum.Font.SourceSansBold btn.TextSize = 14 Instance.new("UICorner", btn) local frame = Instance.new("Frame", sg) frame.Size = UDim2.new(0, 360, 0, 200) frame.Position = UDim2.new(0.3, 0, 0.2, 0) frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25) frame.Visible = true frame.Active = true frame.Draggable = true Instance.new("UICorner", frame) local tl = Instance.new("TextLabel", frame) tl.Size = UDim2.new(1, 0, 0, 35) tl.BackgroundColor3 = Color3.fromRGB(35, 35, 35) tl.Text = "  SAMMERHUB BEAM BEPASS V6" tl.TextColor3 = Color3.fromRGB(255, 255, 255) tl.TextXAlignment = Enum.TextXAlignment.Left tl.Font = Enum.Font.SourceSansBold tl.TextSize = 14 Instance.new("UICorner", tl) local t1 = Instance.new("TextButton", frame) t1.Size = UDim2.new(0, 150, 0, 35) t1.Position = UDim2.new(0.05, 0, 0.35, 0) t1.BackgroundColor3 = Color3.fromRGB(150, 40, 40) t1.Text = "Aimbot: OFF" t1.TextColor3 = Color3.fromRGB(255, 255, 255) t1.Font = Enum.Font.SourceSansBold t1.TextSize = 14 t1.MouseButton1Click:Connect(function() Config.Aimbot = not Config.Aimbot t1.BackgroundColor3 = Config.Aimbot and Color3.fromRGB(40, 150, 40) or Color3.fromRGB(150, 40, 40) t1.Text = Config.Aimbot and "Aimbot: ON" or "Aimbot: OFF" end) local t2 = Instance.new("TextButton", frame) t2.Size = UDim2.new(0, 150, 0, 35) t2.Position = UDim2.new(0.53, 0, 0.35, 0) t2.BackgroundColor3 = Color3.fromRGB(150, 40, 40) t2.Text = "ESP Wall: OFF" t2.TextColor3 = Color3.fromRGB(255, 255, 255) t2.Font = Enum.Font.SourceSansBold t2.TextSize = 14 t2.MouseButton1Click:Connect(function() Config.ESPEnabled = not Config.ESPEnabled t2.BackgroundColor3 = Config.ESPEnabled and Color3.fromRGB(40, 150, 40) or Color3.fromRGB(150, 40, 40) t2.Text = Config.ESPEnabled and "ESP Wall: ON" or "ESP Wall: OFF" end) local inp = Instance.new("TextBox", frame) inp.Size = UDim2.new(0, 150, 0, 35) inp.Position = UDim2.new(0.05, 0, 0.65, 0) inp.BackgroundColor3 = Color3.fromRGB(45, 45, 45) inp.Text = "Smooth: 0.05" inp.TextColor3 = Color3.fromRGB(255, 255, 255) inp.Font = Enum.Font.SourceSans inp.TextSize = 13 inp.FocusLost:Connect(function() local v = tonumber(inp.Text:match("[%d%.]+")) if v then Config.Smoothness = math.clamp(v, 0.001, 1) inp.Text = "Smooth: "..tostring(Config.Smoothness) end end) local inpFov = Instance.new("TextBox", frame) inpFov.Size = UDim2.new(0, 150, 0, 35) inpFov.Position = UDim2.new(0.53, 0, 0.65, 0) inpFov.BackgroundColor3 = Color3.fromRGB(45, 45, 45) inpFov.Text = "FOV: 120" inpFov.TextColor3 = Color3.fromRGB(255, 255, 255) inpFov.Font = Enum.Font.SourceSans inpFov.TextSize = 13 inpFov.FocusLost:Connect(function() local v = tonumber(inpFov.Text:match("%d+")) if v then Config.FOVRadius = math.clamp(v, 20, 400) fovCircle.Size = UDim2.fromOffset(Config.FOVRadius * 2, Config.FOVRadius * 2) fovCircle.Position = UDim2.new(0.5, -Config.FOVRadius, 0.5, -Config.FOVRadius) inpFov.Text = "FOV: "..tostring(Config.FOVRadius) end end) btn.MouseButton1Click:Connect(function() frame.Visible = not frame.Visible end) RunService.RenderStepped:Connect(function() fovCircle.Visible = Config.FOVEnabled local target = GetTarget() if Config.Aimbot and target then Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Position), Config.Smoothness) end camAtt.WorldPosition = Camera.CFrame.Position + (Camera.CFrame.LookVector * 2) + (Camera.CFrame.UpVector * 1.5) local function applyESP(char) if char and char ~= LocalPlayer.Character then local root = GetDeagleTarget(char) if root then local bb = char:FindFirstChildOfClass("BillboardGui") local beam = char:FindFirstChild("SnapBeam") local tAtt = root:FindFirstChild("TargetAtt") if Config.ESPEnabled then if not bb then bb = Instance.new("BillboardGui", char) bb.AlwaysOnTop = true bb.Size = UDim2.new(4, 0, 5, 0) bb.Adornee = root local f = Instance.new("Frame", bb) f.Size = UDim2.new(1, 0, 1, 0) f.BackgroundTransparency = 1 local s = Instance.new("UIStroke", f) s.Thickness = 2 s.Color = Color3.fromRGB(255, 0, 0) end bb.Enabled = true if not tAtt then tAtt = Instance.new("Attachment", root) tAtt.Name = "TargetAtt" end if not beam then beam = Instance.new("Beam", char) beam.Name = "SnapBeam" beam.Width0 = 0.05 beam.Width1 = 0.05 beam.Color = ColorSequence.new(Color3.fromRGB(255, 0, 0)) beam.FaceCamera = true beam.Attachment0 = camAtt beam.Attachment1 = tAtt end beam.Enabled = true else if bb then bb.Enabled = false end if beam then beam.Enabled = false end end end end end for _, p in ipairs(Players:GetPlayers()) do applyESP(p.Character) end for _, obj in ipairs(workspace:GetChildren()) do applyESP(obj) end local alive = workspace:FindFirstChild("Alive") if alive then for _, obj in ipairs(alive:GetChildren()) do applyESP(obj) end end end)
